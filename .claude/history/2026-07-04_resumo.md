# Histórico — 04/07/2026

## [claude - Victor Fausto] 04/07/2026 — HH:MM
**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- app/supabase/migrations/0002_goals.sql (criado — tabela goals + RLS + trigger + uniques; rodada via psql)
- app/src/types/database.ts (modificado — GoalRow/GoalKind + entrada goals)
- app/src/features/goals/api.ts (criado — CRUD de metas + goalsForMonth)
- app/src/features/goals/GoalFormModal.tsx (criado — tipo Limite/Alvo, categoria, valor, recorrência)
- app/src/features/insights/engine.ts (criado — 10+ geradores puros de insight c/ scopes e prioridade)
- app/src/features/insights/InsightsCarousel.tsx (criado — carrossel paginado reutilizável + useInsights)
- app/src/app/(app)/(tabs)/goals.tsx (reescrito — lista de metas c/ barra de progresso, CRUD, carrossel)
- app/src/app/(app)/(tabs)/index.tsx (modificado — carrossel de avisos de meta)
- app/src/app/(app)/(tabs)/charts.tsx (modificado — carrossel de cards independentes do filtro global)
- app/src/app/(app)/expense/[id].tsx (modificado — hint "gastou R$X · restam R$Y" na categoria com meta)
**O que foi feito:** Feature completa de Metas (limite de gasto e alvo a bater, recorrente por
padrão, RLS) + engine de insights client-side (1 query all-time cacheada + geradores puros:
progresso/estouro de meta, conquista, gasto do ano por categoria, mês recorde, sazonalidade,
tendência 3m, maior compra, média diária, dia da semana, % em aberto) exposta por um carrossel
reutilizável com scopes por tela (despesas/graficos/metas).
**Critério de aceite:** ✅ Validado no emulador (screenshots): tela Metas com card "vá mais
devagar" + barra 61% (R$6,11 de R$10); aviso na Despesas; hint no form ("restam R$3,89 do
limite de R$10,00"); carrossel dos Gráficos com cards de ano/média diária e paginação.
`tsc --noEmit` limpo. Meta de exemplo (Entretenimento, limite R$10) criada no banco p/ validação.
**Status:** ✅ Concluído
