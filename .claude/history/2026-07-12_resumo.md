## [claude - Victor Fausto] 12/07/2026 — 20:20
**Comando:** /dev
**Complexidade:** 🟡 Média
**Arquivos tocados:**
- import_despesas_valdeir.sql (criado — não versionado)
- .gitignore (criado)
**O que foi feito:** Validação do `vitim.json` (160 despesas, 04/12/2025→09/07/2026, R$ 19.031,53) contra o schema `d20_despesas.expenses` e import transacional e idempotente para a conta `valdeirpratasilva@gmail.com`. Descoberto que o `DIRECT_CONNECTION` do `.env` é IPv6-only; conexão feita via pooler IPv4 `aws-1-us-east-2.pooler.supabase.com:5432`. Adicionado `.gitignore` na raiz para impedir que o extrato financeiro pessoal e o SQL gerado entrem no repositório.
**Critério de aceite:** ✅ Conta confirmada em `auth.users`. Dry-run em ROLLBACK conferido antes do COMMIT (números idênticos). 160 despesas inseridas, 0 categorias criadas (as 14 padrão já existiam). Conta com 161 despesas / R$ 19.041,53, validado por SELECT agrupado por mês.
**Status:** ✅ Concluído

**Notas para o dev:**
- `DIRECT_CONNECTION` no `.env` aponta para `db.<ref>.supabase.co`, que só resolve em IPv6 e dá timeout nesta máquina. O pooler IPv4 do projeto é `aws-1-us-east-2.pooler.supabase.com:5432` (prefixo `aws-1`, não `aws-0`). Vale adicionar como `POOLER_CONNECTION` no `.env.example`.
- Duplicata `Celular pai - Mercado Livre - 4/10` (2x em 04/06/2026, R$ 59,55) entrou duas vezes porque constava duplicada no JSON. Conferir com o Vitor e apagar uma no app se for erro de extração.
- Os 4 itens com `recorrente: true` (Linkedin e outros) entraram como despesa comum, `recurrent_id` nulo. Marcar recorrência no app se desejado.
