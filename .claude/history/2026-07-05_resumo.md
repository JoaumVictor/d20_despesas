## [claude - Victor Fausto] 05/07/2026 — 00:42
**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- app/supabase/migrations/0004_reminders.sql (criado)
- app/src/types/database.ts (modificado)
- app/src/features/reminders/api.ts (criado)
- app/src/features/reminders/ReminderFormModal.tsx (criado)
- app/src/features/reminders/PayReminderSheet.tsx (criado)
- app/src/features/reminders/ReminderCard.tsx (criado)
- app/src/features/suggestions/engine.ts (modificado)
- app/src/features/suggestions/SuggestionCard.tsx (modificado)
- app/src/features/suggestions/useSuggestions.ts (criado)
- app/src/app/(app)/(tabs)/goals.tsx (modificado)
- app/src/app/(app)/(tabs)/_layout.tsx (modificado)
**O que foi feito:** A tab "Metas" virou "Foco", agora dividida em sub-abas Metas/Lembretes. Lembretes são contas fixas mensais (nome, categoria, dia de vencimento) com fluxo "Paguei" que deixa escolher entre criar a despesa real ou só marcar como concluído (estado guardado em `reminder_completions`, resetando sozinho a cada mês novo, no mesmo padrão de "fantasmas" já usado nas parcelas). Sugestões de recorrência passaram a propor "criar lembrete mensal" (com dia sugerido calculado a partir do histórico) em vez de lançar a despesa direto, e viraram uma seção no rodapé da lista de Lembretes (não mais uma sub-aba própria), com um pontinho vermelho de indicação na navbar (ícone Foco) e no segmento Lembretes sempre que houver sugestão pendente.
**Critério de aceite:** ✅ tsc --noEmit limpo; testado no emulador Android — criação de meta e de lembrete abrindo corretamente (bug inicial era só a Activity nativa dessincronizada após vários reloads via dev-menu, resolvido com restart completo do app), navegação entre sub-abas Metas/Lembretes funcionando, tab renomeada para "Foco" na navbar.
**Status:** ✅ Concluído
